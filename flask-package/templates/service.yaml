apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-package.fullname" . }}-service
  labels:
    {{- include "flask-package.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "flask-package.fullname" . }}